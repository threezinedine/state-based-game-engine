#pragma once
#include <NTTEngine/defines.hpp>
#include <NTTEngine/structures/position.hpp>

namespace ntt::input
{
    /**
     * This is used only for testing purpose, user does not use it
     */
    void SetMousePosition(const Position &position);
} // namespace ntt::input
